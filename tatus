[1mdiff --git a/apps/system-app/package.json b/apps/system-app/package.json[m
[1mindex 6bb9cc1..562c050 100644[m
[1m--- a/apps/system-app/package.json[m
[1m+++ b/apps/system-app/package.json[m
[36m@@ -3,6 +3,12 @@[m
   "version": "1.0.0",[m
   "private": true,[m
   "type": "module",[m
[32m+[m[32m  "sideEffects": [[m
[32m+[m[32m    "src/services/eps.ts",[m
[32m+[m[32m    "src/App.vue",[m
[32m+[m[32m    "**/*.vue",[m
[32m+[m[32m    "virtual:eps"[m
[32m+[m[32m  ],[m
   "scripts": {[m
     "predev": "pnpm --filter @btc/vite-plugin build && pnpm check:circular",[m
     "dev": "vite --port 8080",[m
[1mdiff --git a/apps/system-app/src/services/eps.ts b/apps/system-app/src/services/eps.ts[m
[1mindex c742d8f..05f88b2 100644[m
[1m--- a/apps/system-app/src/services/eps.ts[m
[1m+++ b/apps/system-app/src/services/eps.ts[m
[36m@@ -1,7 +1,11 @@[m
[32m+[m[32m// 注意：此文件被标记为副作用模块，确保完整的 EPS 数据被包含[m
[32m+[m[32m// 防止 tree-shaking 移除未直接访问的 EPS 服务[m
 import epsModule, { service as rawService, list as rawList } from 'virtual:eps';[m
 [m
 const service = rawService ?? (epsModule as any)?.service ?? epsModule ?? {};[m
 const list = rawList ?? (epsModule as any)?.list ?? [];[m
 [m
[32m+[m[32m// 确保完整的 service 对象被导出和使用[m
[32m+[m[32m// 通过直接导出整个对象，而不是部分属性，防止 tree-shaking[m
 export { service, list };[m
 export default service;[m
[1mdiff --git a/apps/system-app/vite.config.ts b/apps/system-app/vite.config.ts[m
[1mindex fecc4f8..6112c51 100644[m
[1m--- a/apps/system-app/vite.config.ts[m
[1m+++ b/apps/system-app/vite.config.ts[m
[36m@@ -207,6 +207,19 @@[m [mexport default defineConfig({[m
     // 主应用不应该使用 external，所有依赖都应该被打包[m
     // external 配置仅用于子应用（微前端场景）[m
     rollupOptions: {[m
[32m+[m[32m      // 防止 tree-shaking 移除 virtual:eps 相关代码[m
[32m+[m[32m      treeshake: {[m
[32m+[m[32m        moduleSideEffects(id) {[m
[32m+[m[32m          // 保留 virtual:eps 虚拟模块和 EPS 服务文件的所有副作用[m
[32m+[m[32m          // 这样可以防止 tree-shaking 移除未直接访问的 EPS 服务属性[m
[32m+[m[32m          if (id.includes('virtual:eps') || id.includes('services/eps') || id.includes('/eps.ts')) {[m
[32m+[m[32m            return true;[m
[32m+[m[32m          }[m
[32m+[m[32m          return false;[m
[32m+[m[32m        },[m
[32m+[m[32m        // 保留所有属性访问（包括可选链），防止部分服务被移除[m
[32m+[m[32m        propertyReadSideEffects: true,[m
[32m+[m[32m      },[m
       output: {[m
         // 确保相对路径的 import 使用正确的 base URL[m
         // 这样在微前端场景下，资源路径会正确解析[m
[36m@@ -215,7 +228,19 @@[m [mexport default defineConfig({[m
         // 这样浏览器会根据当前模块的位置解析，而不是根据页面 URL[m
         preserveModules: false,[m
         manualChunks(id) {[m
[31m-          // 将 EPS 数据单独拆分，便于线上排查[m
[32m+[m[32m          // ========================================[m
[32m+[m[32m          // EPS 代码分割配置[m
[32m+[m[32m          // ========================================[m
[32m+[m[32m          // 将 EPS (Endpoint Service) 数据单独拆分，便于线上排查和调试[m
[32m+[m[32m          //[m[41m [m
[32m+[m[32m          // 说明：[m
[32m+[m[32m          // 1. virtual:eps 是虚拟模块，由 @btc/vite-plugin 的 epsPlugin 生成[m
[32m+[m[32m          // 2. 所有导入 virtual:eps 的模块（如 App.vue、services/eps.ts）会被打包到同一个 chunk[m
[32m+[m[32m          // 3. chunk 名称：eps-contract，最终生成文件：eps-contract-[hash].js[m
[32m+[m[32m          // 4. 如果构建产物中没有 eps-contract-*.js 文件，说明：[m
[32m+[m[32m          //    - EPS 数据未生成（检查 build/eps 目录）[m
[32m+[m[32m          //    - 或者 virtual:eps 未被正确导入[m
[32m+[m[32m          //    - 或者 EPS 数据为空（所有代码被 tree-shaking 移除）[m
           if (id.includes('virtual:eps')) {[m
             return 'eps-contract';[m
           }[m
