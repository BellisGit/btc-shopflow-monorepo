/**
 * 品质模块配置
 */

import type { PageConfig } from '../../../../../types/locale';

export default {
  locale: {
    'zh-CN': {
      // 菜单配置（暂时为空，待后续添加）
    },
    'en-US': {
      // 菜单配置（暂时为空，待后续添加）
    },
  },

  columns: {},

  forms: {},

  service: {},
} satisfies PageConfig;
