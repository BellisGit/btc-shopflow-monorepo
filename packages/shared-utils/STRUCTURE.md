# @btc/shared-utils 工具包结构

## 📁 目录结构

```
src/
├── 📂 核心工具模块
│   ├── date/          # 日期时间工具
│   ├── format/        # 格式化工具
│   ├── validate/      # 验证工具
│   └── storage/       # 存储工具
├── 📂 数据处理模块
│   ├── array/         # 数组工具
│   ├── object/        # 对象工具
│   ├── string/        # 字符串工具
│   └── number/        # 数字工具
└── 📂 业务功能模块
    ├── form/          # 表单工具
    ├── hooks/         # Vue组合式函数
    └── http/          # HTTP响应拦截器
```

## 🛠️ 工具分类详情

### 📅 日期时间工具 (`date/`)
- `formatDate` - 格式化日期
- `formatDateTime` - 格式化日期时间
- `formatDateTimeFriendly` - 用户友好的日期时间格式化
- `isDateTimeField` - 检查是否为时间字段
- `getDateRange` - 获取日期范围
- `dateDiff` - 计算日期差

### 🎨 格式化工具 (`format/`)
- `formatMoney` - 格式化金额
- `formatNumber` - 格式化数字（千分位）
- `formatFileSize` - 格式化文件大小
- `formatPercent` - 格式化百分比
- `hidePhone` - 隐藏手机号中间4位
- `hideIdCard` - 隐藏身份证号中间部分
- `formatTableNumber` - 格式化表格数字显示

### ✅ 验证工具 (`validate/`)
- `isEmail` - 验证邮箱
- `isPhone` - 验证手机号
- `isIdCard` - 验证身份证号
- `isUrl` - 验证URL
- `isEmpty` - 验证是否为空

### 💾 存储工具 (`storage/`)
- `StorageUtil` 类 - 本地存储工具类
- 支持过期时间、键名管理等功能

### 📋 数组工具 (`array/`)
- `sortByLocale` - 本地化排序
- `unique` - 数组去重
- `groupBy` - 数组分组
- `chunk` - 数组分块
- `flatten` / `flattenDeep` - 数组扁平化
- `intersection` / `difference` / `union` - 数组集合操作
- `shuffle` / `sample` - 随机操作
- `sum` / `average` / `maxBy` / `minBy` - 数学统计

### 🔧 对象工具 (`object/`)
- `deepClone` - 深拷贝对象
- `get` - 获取对象属性值（支持路径）
- `pick` - 选择对象的指定属性
- `omit` - 排除对象的指定属性

### 📝 字符串工具 (`string/`)
- `capitalize` - 首字母大写
- `camelCase` - 驼峰命名转换
- `kebabCase` - 短横线命名转换
- `truncate` - 截断字符串
- `randomString` - 生成随机字符串

### 🔢 数字工具 (`number/`)
- `toPrecision` - 数字精度处理
- `clamp` - 数字范围限制
- `random` - 随机数生成
- `isEven` / `isOdd` - 奇偶数判断
- `padZero` - 数字补零

### 📝 表单工具 (`form/`)
- 表单数据绑定和提交转换
- 8种内置转换器

### 🎣 Vue组合式函数 (`hooks/`)
- `useMessage` - 统一的消息发送接口

### 🌐 HTTP工具 (`http/`)
- 响应拦截器 - API响应统一处理

## 📦 使用方式

```typescript
// 导入所有工具
import * as utils from '@btc/shared-utils';

// 按需导入
import { formatDate, capitalize, unique } from '@btc/shared-utils';

// 分类导入
import { formatDate } from '@btc/shared-utils/date';
import { capitalize } from '@btc/shared-utils/string';
import { unique } from '@btc/shared-utils/array';
```

## 🔄 改进历程

### v1.1.0 - 结构重组
- ✅ 创建了清晰的目录分类结构
- ✅ 将原有的单文件工具移动到对应目录
- ✅ 新增了 `string/`、`number/`、`object/` 工具模块
- ✅ 大幅增强了 `array/` 工具功能
- ✅ 修复了所有中文注释乱码问题
- ✅ 更新了入口文件导出结构

### 优势
1. **分类清晰** - 按功能领域分类，便于查找和维护
2. **模块化设计** - 每个分类独立，可按需导入
3. **功能完整** - 覆盖了常见的工具函数需求
4. **类型安全** - 完整的 TypeScript 类型定义
5. **易于扩展** - 结构化设计便于后续添加新功能
